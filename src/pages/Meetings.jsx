// -------------------------------------------------
// | Meeting Header(Title + Date + Project)
// -------------------------------------------------

// | Tabs: [Overview][Actions][Sentiment][Chat]
// -------------------------------------------------

// (Tab Content Changes)
// | Responsible | Task | Deadline | Status |
// Export CSV button
import React from 'react'

const Meetings = () => {
    return (
        <div>Meetings</div>
    )
}

export default Meetings